{% extends 'base.html.twig' %}


{% block body %}
<div class="row-span-1 items-center mx-auto  mt-28">
<p class="text-lg font-semibold text-[#2B292D]">
	If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.
	        This link will expire in
	{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
</p>
<p class="text-lg font-semibold text-[#2B292D]">If you don't receive an email please check your spam folder or</p>


<a class="text-lg font-semibold text-[#2B292D]" href="{{ path('app_forgot_password_request') }}">try again</a>.</p>


</div>
    


{% endblock %}
